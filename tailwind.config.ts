/** @type {import('tailwindcss').Config} */
const config = {
  content: ["./src/**/*.{ts,tsx}"],
  darkMode: "class", // <--- importante!
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
